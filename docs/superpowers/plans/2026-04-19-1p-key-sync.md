# 1Password Key Sync 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 model_api_connection 的密钥来源从散落的 `.env` 迁移到"1Password SSoT + `~/.config/llm/keys.env` 本地缓存"模式，让任意项目 / cron 任务都能零配置调用。

**Architecture:** 新增 `llm-sync-keys` 命令把 1P 条目拉到本地缓存；connector 固定加载该缓存，无视 CWD；消费方只需 `uv add --editable` 即可调用。详见 `docs/superpowers/specs/2026-04-19-1p-key-sync-redesign.md`。

**Tech Stack:** Python 3.10+, litellm, python-dotenv, 1Password CLI (`op`), pytest, uv。

---

## 文件结构

**新增：**
- `paths.py` — 路径常量（`KEYS_ENV_DIR`、`KEYS_ENV_PATH`、`CONFIG_PATH`）
- `key_sync.py` — `llm-sync-keys` 命令实现
- `tests/test_key_sync.py` — `key_sync.py` 的单元测试

**修改：**
- `model_connector.py` — 改为固定路径 `load_dotenv`；修 singleton bug；加 `raw=True`；改进错误信息；挪 `CONFIG_PATH` 到 `paths.py`
- `models_config.json` — 每个 provider 新增 `op_reference` 字段
- `pyproject.toml` — 加 `[project.scripts]` 注册 `llm-sync-keys`；`py-modules` 补 `paths`、`key_sync`
- `README.md` — 替换 `.env` 流程章节，新增 1P + `llm-sync-keys` 说明、新 Mac bootstrap checklist

**删除：**
- `.env.example` — 被 1P vault + `llm-sync-keys --help` 替代
- `.env`（本地明文，迁移完成后删）

---

## Task 1: 新增 `paths.py` 路径常量

**Files:**
- Create: `paths.py`

- [ ] **Step 1: 创建 `paths.py`**

写入 `$WORKSPACE_ROOT/model_api_connection/paths.py`：

```python
"""Shared filesystem path constants for model_api_connection."""

from pathlib import Path

# Local cache of API keys (populated by `llm-sync-keys`).
KEYS_ENV_DIR = Path.home() / ".config" / "llm"
KEYS_ENV_PATH = KEYS_ENV_DIR / "keys.env"

# Provider registry lives next to this module.
CONFIG_PATH = Path(__file__).parent / "models_config.json"
```

- [ ] **Step 2: 校验 import 通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run python -c "from paths import KEYS_ENV_PATH, KEYS_ENV_DIR, CONFIG_PATH; print(KEYS_ENV_PATH); print(CONFIG_PATH)"`

Expected 输出：
```
$HOME/.config/llm/keys.env
$WORKSPACE_ROOT/model_api_connection/models_config.json
```

- [ ] **Step 3: Commit**

```bash
cd $WORKSPACE_ROOT/model_api_connection
git add paths.py
git commit -m "$(cat <<'EOF'
feat: add paths.py with KEYS_ENV_PATH and CONFIG_PATH constants

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 2: 在 `models_config.json` 加 `op_reference`

**Files:**
- Modify: `models_config.json`

- [ ] **Step 1: 给每个 provider 加 `op_reference` 字段**

用 Edit 工具把每个 provider block 里加一行 `op_reference`。完整改后内容：

```json
{
  "providers": {
    "openai": {
      "api_key_env": "OPENAI_API_KEY",
      "op_reference": "op://llmkeys/OpenAI/credential",
      "default_model": "gpt-4o",
      "models": {
        "gpt-4o":      "openai/gpt-4o",
        "gpt-4o-mini": "openai/gpt-4o-mini",
        "o1":          "openai/o1",
        "o3":          "openai/o3",
        "o3-mini":     "openai/o3-mini",
        "o4-mini":     "openai/o4-mini"
      }
    },
    "anthropic": {
      "api_key_env": "ANTHROPIC_API_KEY",
      "op_reference": "op://llmkeys/Anthropic/credential",
      "default_model": "sonnet-4.6",
      "models": {
        "opus-4.6":   "anthropic/claude-opus-4-6",
        "sonnet-4.6": "anthropic/claude-sonnet-4-6",
        "haiku-4.5":  "anthropic/claude-haiku-4-5-20251001"
      }
    },
    "gemini": {
      "api_key_env": "GEMINI_API_KEY",
      "op_reference": "op://llmkeys/Gemini/credential",
      "default_model": "gemini-2.5-flash",
      "models": {
        "gemini-2.5-flash":                    "gemini/gemini-2.5-flash",
        "gemini-3.1-pro-preview":              "gemini/gemini-3.1-pro-preview",
        "gemini-3.1-pro-preview-customtools":  "gemini/gemini-3.1-pro-preview-customtools",
        "gemini-3-pro-preview":                "gemini/gemini-3-pro-preview"
      }
    },
    "siliconflow": {
      "api_key_env": "SILICONFLOW_API_KEY",
      "op_reference": "op://llmkeys/SiliconFlow/credential",
      "base_url": "https://api.siliconflow.cn/v1",
      "default_model": "Kimi-K2.5",
      "models": {
        "deepseek-v3":                      "openai/deepseek-ai/DeepSeek-V3",
        "deepseek-r1":                      "openai/deepseek-ai/DeepSeek-R1",
        "Qwen3.5-397B-A17B":               "openai/Qwen/Qwen3.5-397B-A17B",
        "Qwen3-VL-32B":                    "openai/Qwen/Qwen3-VL-32B-Instruct",
        "Qwen2.5-VL-72B":                  "openai/Qwen/Qwen2.5-VL-72B-Instruct",
        "deepseek-vl2":                    "openai/deepseek-ai/deepseek-vl2",
        "Qwen3-Coder-480B-A35B-Instruct":  "openai/Qwen/Qwen3-Coder-480B-A35B-Instruct",
        "QwQ-32B":                          "openai/Qwen/QwQ-32B",
        "Qwen2.5-32B-Instruct":            "openai/Qwen/Qwen2.5-32B-Instruct",
        "Kimi-K2-Instruct-0905":           "openai/moonshotai/Kimi-K2-Instruct-0905",
        "Kimi-K2.5":                        "openai/Pro/moonshotai/Kimi-K2.5",
        "MiniMax-M2.5":                     "openai/Pro/MiniMaxAI/MiniMax-M2.5",
        "GLM-4.6":                          "openai/zai-org/GLM-4.6",
        "SenseVoiceSmall":                  "openai/FunAudioLLM/SenseVoiceSmall"
      }
    },
    "poe": {
      "api_key_env": "POE_API_KEY",
      "op_reference": "op://llmkeys/Poe/credential",
      "default_model": "Claude-Sonnet-4.5",
      "models": {
        "Claude-Opus-4.5":   "poe/Claude-Opus-4.5",
        "Claude-Sonnet-4.5": "poe/Claude-Sonnet-4.5",
        "Claude-Haiku-4.5":  "poe/Claude-Haiku-4.5",
        "Gemini-3-Pro":      "poe/Gemini-3-Pro",
        "GPT-4o":            "poe/GPT-4o",
        "Grok-4":            "poe/Grok-4",
        "Llama-3.1-405B":    "poe/Llama-3.1-405B"
      }
    }
  }
}
```

- [ ] **Step 2: 校验 JSON 合法**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run python -c "import json; json.load(open('models_config.json')); print('ok')"`

Expected: `ok`

- [ ] **Step 3: Commit**

```bash
git add models_config.json
git commit -m "$(cat <<'EOF'
feat: add op_reference field to each provider in models_config.json

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 3: TDD `load_providers()` 函数

**Files:**
- Create: `tests/test_key_sync.py`
- Create: `key_sync.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_key_sync.py` 新建文件，内容：

```python
"""Tests for key_sync module."""

import json
from pathlib import Path

import pytest

from key_sync import load_providers


def test_load_providers_returns_provider_with_op_reference(tmp_path):
    cfg = {
        "providers": {
            "openai": {
                "api_key_env": "OPENAI_API_KEY",
                "op_reference": "op://llmkeys/OpenAI/credential",
            },
        }
    }
    config_path = tmp_path / "models_config.json"
    config_path.write_text(json.dumps(cfg), encoding="utf-8")

    result = load_providers(config_path)

    assert result == {
        "openai": ("op://llmkeys/OpenAI/credential", "OPENAI_API_KEY"),
    }


def test_load_providers_skips_provider_without_op_reference(tmp_path):
    cfg = {
        "providers": {
            "openai": {
                "api_key_env": "OPENAI_API_KEY",
                "op_reference": "op://llmkeys/OpenAI/credential",
            },
            "legacy": {
                "api_key_env": "LEGACY_API_KEY"
            },
        }
    }
    config_path = tmp_path / "models_config.json"
    config_path.write_text(json.dumps(cfg), encoding="utf-8")

    result = load_providers(config_path)

    assert "openai" in result
    assert "legacy" not in result


def test_load_providers_filter_by_name(tmp_path):
    cfg = {
        "providers": {
            "openai": {
                "api_key_env": "OPENAI_API_KEY",
                "op_reference": "op://llmkeys/OpenAI/credential",
            },
            "anthropic": {
                "api_key_env": "ANTHROPIC_API_KEY",
                "op_reference": "op://llmkeys/Anthropic/credential",
            },
        }
    }
    config_path = tmp_path / "models_config.json"
    config_path.write_text(json.dumps(cfg), encoding="utf-8")

    result = load_providers(config_path, only="openai")

    assert list(result.keys()) == ["openai"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: `ModuleNotFoundError: No module named 'key_sync'` 或 `ImportError`。

- [ ] **Step 3: 写 `key_sync.py` 骨架 + `load_providers`**

创建 `$WORKSPACE_ROOT/model_api_connection/key_sync.py`：

```python
"""llm-sync-keys: sync API keys from 1Password to ~/.config/llm/keys.env."""

from __future__ import annotations

import json
from pathlib import Path


def load_providers(
    config_path: Path, only: str | None = None
) -> dict[str, tuple[str, str]]:
    """Read models_config.json and return providers that have an op_reference.

    Returns a dict mapping provider name -> (op_reference, api_key_env).
    When `only` is provided, the result contains at most that one provider.
    """
    cfg = json.loads(config_path.read_text(encoding="utf-8"))
    result: dict[str, tuple[str, str]] = {}
    for name, spec in cfg.get("providers", {}).items():
        if only is not None and name != only:
            continue
        ref = spec.get("op_reference")
        env_var = spec.get("api_key_env")
        if ref and env_var:
            result[name] = (ref, env_var)
    return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 3 passed。

- [ ] **Step 5: Commit**

```bash
git add key_sync.py tests/test_key_sync.py
git commit -m "$(cat <<'EOF'
feat: add load_providers() in key_sync with TDD

Reads models_config.json and returns providers that declare an
op_reference, optionally filtered by name.

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 4: TDD `fetch_key()` 函数

**Files:**
- Modify: `tests/test_key_sync.py`
- Modify: `key_sync.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_key_sync.py` 末尾追加：

```python
from unittest.mock import MagicMock, patch

from key_sync import fetch_key, OpError


def test_fetch_key_returns_stripped_stdout():
    with patch("key_sync.subprocess.run") as mock_run:
        mock_run.return_value = MagicMock(
            returncode=0, stdout="sk-12345\n", stderr=""
        )
        key = fetch_key("op://llmkeys/OpenAI/credential")
        assert key == "sk-12345"
        mock_run.assert_called_once_with(
            ["op", "read", "op://llmkeys/OpenAI/credential"],
            capture_output=True,
            text=True,
            check=False,
        )


def test_fetch_key_raises_oper_on_failure():
    with patch("key_sync.subprocess.run") as mock_run:
        mock_run.return_value = MagicMock(
            returncode=1, stdout="", stderr="error reading item"
        )
        with pytest.raises(OpError) as excinfo:
            fetch_key("op://llmkeys/Missing/credential")
        assert "error reading item" in str(excinfo.value)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: `ImportError: cannot import name 'fetch_key'` 或 `'OpError'`。

- [ ] **Step 3: 实现 `fetch_key` 和 `OpError`**

在 `key_sync.py` 顶部 import 处补：

```python
import subprocess
```

在 `load_providers` 之前（即紧跟 import 之后）加：

```python
class OpError(RuntimeError):
    """Raised when `op` CLI returns a non-zero exit code."""
```

在 `load_providers` 之后追加：

```python
def fetch_key(op_reference: str) -> str:
    """Fetch a single credential from 1Password via the `op` CLI.

    Raises OpError if `op` exits non-zero.
    """
    result = subprocess.run(
        ["op", "read", op_reference],
        capture_output=True,
        text=True,
        check=False,
    )
    if result.returncode != 0:
        raise OpError(result.stderr.strip() or "op read failed")
    return result.stdout.strip()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 5 passed（3 旧 + 2 新）。

- [ ] **Step 5: Commit**

```bash
git add key_sync.py tests/test_key_sync.py
git commit -m "$(cat <<'EOF'
feat: add fetch_key() and OpError in key_sync

Invokes `op read` via subprocess, raising OpError on non-zero exit.

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 5: TDD `write_keys_env()` 原子写入

**Files:**
- Modify: `tests/test_key_sync.py`
- Modify: `key_sync.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_key_sync.py` 末尾追加：

```python
import os

from key_sync import write_keys_env


def test_write_keys_env_creates_file_with_mode_600(tmp_path):
    target = tmp_path / "llm" / "keys.env"

    write_keys_env({"OPENAI_API_KEY": "sk-abc"}, target)

    assert target.exists()
    mode = target.stat().st_mode & 0o777
    assert mode == 0o600
    assert target.read_text(encoding="utf-8") == "OPENAI_API_KEY=sk-abc\n"


def test_write_keys_env_creates_parent_dir_with_mode_700(tmp_path):
    target = tmp_path / "llm" / "keys.env"

    write_keys_env({"OPENAI_API_KEY": "sk-abc"}, target)

    parent_mode = target.parent.stat().st_mode & 0o777
    assert parent_mode == 0o700


def test_write_keys_env_overwrites_existing(tmp_path):
    target = tmp_path / "keys.env"
    target.write_text("OLD=value\n", encoding="utf-8")

    write_keys_env({"NEW": "v"}, target)

    assert target.read_text(encoding="utf-8") == "NEW=v\n"


def test_write_keys_env_preserves_insertion_order(tmp_path):
    target = tmp_path / "keys.env"

    write_keys_env(
        {"A_KEY": "a", "B_KEY": "b", "C_KEY": "c"}, target
    )

    assert target.read_text(encoding="utf-8") == "A_KEY=a\nB_KEY=b\nC_KEY=c\n"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: `ImportError: cannot import name 'write_keys_env'`。

- [ ] **Step 3: 实现 `write_keys_env`**

在 `key_sync.py` 顶部 import 处追加：

```python
import os
import tempfile
```

在文件末尾追加：

```python
def write_keys_env(keys: dict[str, str], target: Path) -> None:
    """Atomically write {env_var: value} pairs to target (mode 0600).

    The parent directory is created with mode 0700 if needed.
    """
    target.parent.mkdir(parents=True, exist_ok=True)
    os.chmod(target.parent, 0o700)

    fd, tmp_name = tempfile.mkstemp(
        prefix=".keys.env.", dir=str(target.parent)
    )
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            for env_var, value in keys.items():
                f.write(f"{env_var}={value}\n")
        os.chmod(tmp_name, 0o600)
        os.replace(tmp_name, target)
    except BaseException:
        Path(tmp_name).unlink(missing_ok=True)
        raise
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 9 passed。

- [ ] **Step 5: Commit**

```bash
git add key_sync.py tests/test_key_sync.py
git commit -m "$(cat <<'EOF'
feat: add atomic write_keys_env() with mode 600 / 700

Writes to a temp file then os.replace(), cleaning up on error.

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 6: TDD `main()` — happy path + `--dry-run`

**Files:**
- Modify: `tests/test_key_sync.py`
- Modify: `key_sync.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_key_sync.py` 末尾追加：

```python
import shutil as real_shutil

from key_sync import main


@pytest.fixture
def sample_config(tmp_path):
    cfg = {
        "providers": {
            "openai": {
                "api_key_env": "OPENAI_API_KEY",
                "op_reference": "op://llmkeys/OpenAI/credential",
            },
            "anthropic": {
                "api_key_env": "ANTHROPIC_API_KEY",
                "op_reference": "op://llmkeys/Anthropic/credential",
            },
        }
    }
    config_path = tmp_path / "models_config.json"
    config_path.write_text(json.dumps(cfg), encoding="utf-8")
    return config_path


def test_main_happy_path_writes_all_keys(sample_config, tmp_path, monkeypatch, capsys):
    target = tmp_path / "keys.env"
    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", target)
    monkeypatch.setattr("key_sync.shutil.which", lambda _name: "/usr/local/bin/op")

    call_outputs = {
        "op://llmkeys/OpenAI/credential": "sk-openai\n",
        "op://llmkeys/Anthropic/credential": "sk-ant\n",
    }

    def fake_run(cmd, **_kwargs):
        ref = cmd[-1]
        return MagicMock(returncode=0, stdout=call_outputs[ref], stderr="")

    monkeypatch.setattr("key_sync.subprocess.run", fake_run)

    rc = main([])

    assert rc == 0
    contents = target.read_text(encoding="utf-8")
    assert "OPENAI_API_KEY=sk-openai" in contents
    assert "ANTHROPIC_API_KEY=sk-ant" in contents
    out = capsys.readouterr().out
    assert "Wrote 2 keys" in out


def test_main_dry_run_does_not_call_op_or_write_file(
    sample_config, tmp_path, monkeypatch, capsys
):
    target = tmp_path / "keys.env"
    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", target)

    calls = []

    def fake_run(cmd, **_kwargs):
        calls.append(cmd)
        return MagicMock(returncode=0, stdout="", stderr="")

    monkeypatch.setattr("key_sync.subprocess.run", fake_run)

    rc = main(["--dry-run"])

    assert rc == 0
    assert calls == []
    assert not target.exists()
    out = capsys.readouterr().out
    assert "op://llmkeys/OpenAI/credential" in out
    assert "OPENAI_API_KEY" in out
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: `ImportError: cannot import name 'main'`。

- [ ] **Step 3: 实现 `main`（只做 happy path + dry-run；错误路径下一 task 加）**

在 `key_sync.py` 顶部 import 处追加：

```python
import argparse
import shutil
import sys
```

在最上方 import 之后、`OpError` 之前加：

```python
from paths import CONFIG_PATH, KEYS_ENV_PATH
```

在文件末尾追加：

```python
def _parse_args(argv: list[str] | None) -> argparse.Namespace:
    parser = argparse.ArgumentParser(
        prog="llm-sync-keys",
        description="Sync LLM API keys from 1Password into ~/.config/llm/keys.env.",
    )
    parser.add_argument(
        "--dry-run",
        action="store_true",
        help="List what would be fetched without calling op or writing the cache.",
    )
    parser.add_argument(
        "--provider",
        help="Only sync the named provider (matches a key in models_config.json).",
    )
    return parser.parse_args(argv)


def main(argv: list[str] | None = None) -> int:
    args = _parse_args(argv)
    providers = load_providers(CONFIG_PATH, only=args.provider)
    if not providers:
        print(
            f"No provider with 'op_reference' configured"
            + (f" for --provider {args.provider}" if args.provider else ""),
            file=sys.stderr,
        )
        return 1

    if args.dry_run:
        for name, (ref, env_var) in providers.items():
            print(f"- {name:12s} {ref} -> {env_var}")
        return 0

    if shutil.which("op") is None:
        print(
            "Error: 'op' CLI not installed.\n"
            "Install: brew install 1password-cli",
            file=sys.stderr,
        )
        return 1

    keys: dict[str, str] = {}
    for name, (ref, env_var) in providers.items():
        keys[env_var] = fetch_key(ref)
        print(f"  OK  {name:12s} -> {env_var}")

    write_keys_env(keys, KEYS_ENV_PATH)
    print(f"\nWrote {len(keys)} keys to {KEYS_ENV_PATH} (chmod 600).")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 11 passed。

- [ ] **Step 5: Commit**

```bash
git add key_sync.py tests/test_key_sync.py
git commit -m "$(cat <<'EOF'
feat: add main() in key_sync — happy path and --dry-run

Wires load_providers -> fetch_key -> write_keys_env and prints a
per-provider status line.

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 7: TDD `main()` — 错误路径（缺 op、未认证、部分失败、单 provider 合并）

**Files:**
- Modify: `tests/test_key_sync.py`
- Modify: `key_sync.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_key_sync.py` 末尾追加：

```python
def test_main_missing_op_prints_install_hint(
    sample_config, tmp_path, monkeypatch, capsys
):
    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", tmp_path / "keys.env")
    monkeypatch.setattr("key_sync.shutil.which", lambda _name: None)

    rc = main([])

    assert rc == 1
    err = capsys.readouterr().err
    assert "brew install 1password-cli" in err


def test_main_unauthenticated_op_prints_cli_integration_hint(
    sample_config, tmp_path, monkeypatch, capsys
):
    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", tmp_path / "keys.env")
    monkeypatch.setattr("key_sync.shutil.which", lambda _name: "/usr/local/bin/op")

    def fake_run(_cmd, **_kwargs):
        return MagicMock(
            returncode=1, stdout="", stderr="Error: You are not signed in."
        )

    monkeypatch.setattr("key_sync.subprocess.run", fake_run)

    rc = main([])

    assert rc == 1
    err = capsys.readouterr().err
    assert "Integrate with 1Password CLI" in err


def test_main_partial_failure_writes_remaining_keys_and_exits_nonzero(
    sample_config, tmp_path, monkeypatch, capsys
):
    target = tmp_path / "keys.env"
    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", target)
    monkeypatch.setattr("key_sync.shutil.which", lambda _name: "/usr/local/bin/op")

    def fake_run(cmd, **_kwargs):
        ref = cmd[-1]
        if "OpenAI" in ref:
            return MagicMock(returncode=0, stdout="sk-openai\n", stderr="")
        return MagicMock(returncode=1, stdout="", stderr="item not found")

    monkeypatch.setattr("key_sync.subprocess.run", fake_run)

    rc = main([])

    assert rc == 1
    contents = target.read_text(encoding="utf-8")
    assert "OPENAI_API_KEY=sk-openai" in contents
    assert "ANTHROPIC_API_KEY" not in contents
    err = capsys.readouterr().err
    assert "anthropic" in err.lower()


def test_main_single_provider_preserves_other_keys(
    sample_config, tmp_path, monkeypatch
):
    target = tmp_path / "keys.env"
    target.parent.mkdir(parents=True, exist_ok=True)
    target.write_text(
        "OPENAI_API_KEY=old-openai\nANTHROPIC_API_KEY=old-ant\n",
        encoding="utf-8",
    )
    os.chmod(target, 0o600)

    monkeypatch.setattr("key_sync.CONFIG_PATH", sample_config)
    monkeypatch.setattr("key_sync.KEYS_ENV_PATH", target)
    monkeypatch.setattr("key_sync.shutil.which", lambda _name: "/usr/local/bin/op")

    def fake_run(_cmd, **_kwargs):
        return MagicMock(returncode=0, stdout="sk-new-openai\n", stderr="")

    monkeypatch.setattr("key_sync.subprocess.run", fake_run)

    rc = main(["--provider", "openai"])

    assert rc == 0
    contents = target.read_text(encoding="utf-8")
    assert "OPENAI_API_KEY=sk-new-openai" in contents
    assert "ANTHROPIC_API_KEY=old-ant" in contents
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 4 新测试失败（老的 11 依旧 pass）。

- [ ] **Step 3: 用以下代码**替换 `key_sync.py` 的 `main` 函数（保留之前的 imports、`_parse_args`、`OpError`、`load_providers`、`fetch_key`、`write_keys_env` 不变）：

```python
_AUTH_ERROR_MARKERS = ("not signed in", "you are not signed", "authenticate")


def _read_existing_env(path: Path) -> dict[str, str]:
    if not path.exists():
        return {}
    out: dict[str, str] = {}
    for line in path.read_text(encoding="utf-8").splitlines():
        if not line or line.startswith("#") or "=" not in line:
            continue
        k, _, v = line.partition("=")
        out[k.strip()] = v.strip()
    return out


def main(argv: list[str] | None = None) -> int:
    args = _parse_args(argv)
    providers = load_providers(CONFIG_PATH, only=args.provider)
    if not providers:
        print(
            "No provider with 'op_reference' configured"
            + (f" for --provider {args.provider}" if args.provider else ""),
            file=sys.stderr,
        )
        return 1

    if args.dry_run:
        for name, (ref, env_var) in providers.items():
            print(f"- {name:12s} {ref} -> {env_var}")
        return 0

    if shutil.which("op") is None:
        print(
            "Error: 'op' CLI not installed.\n"
            "Install: brew install 1password-cli",
            file=sys.stderr,
        )
        return 1

    fetched: dict[str, str] = {}
    failures: list[str] = []
    for name, (ref, env_var) in providers.items():
        try:
            fetched[env_var] = fetch_key(ref)
            print(f"  OK  {name:12s} -> {env_var}")
        except OpError as exc:
            msg = str(exc).lower()
            if any(marker in msg for marker in _AUTH_ERROR_MARKERS):
                print(
                    "Error: 'op' CLI is not authenticated.\n"
                    "Open 1Password app -> Settings -> Developer -> "
                    "enable 'Integrate with 1Password CLI'",
                    file=sys.stderr,
                )
                return 1
            print(f"  FAIL {name:12s} {exc}", file=sys.stderr)
            failures.append(name)

    if fetched:
        merged = _read_existing_env(KEYS_ENV_PATH) if args.provider else {}
        merged.update(fetched)
        write_keys_env(merged, KEYS_ENV_PATH)
        print(f"\nWrote {len(merged)} keys to {KEYS_ENV_PATH} (chmod 600).")

    return 1 if failures else 0
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_key_sync.py -v`

Expected: 15 passed。

- [ ] **Step 5: Commit**

```bash
git add key_sync.py tests/test_key_sync.py
git commit -m "$(cat <<'EOF'
feat: handle op missing / unauthenticated / partial failure in main

- print install hint if `op` is not on PATH
- detect 'not signed in' and print CLI-integration instructions
- on partial failure, still write successful keys and exit non-zero
- --provider mode merges with existing keys.env, preserving others

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 8: 注册 `llm-sync-keys` 为 console script

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 编辑 `pyproject.toml`**

把 `[tool.setuptools]` 的 `py-modules` 加上 `paths` 和 `key_sync`，并新增 `[project.scripts]` 节。目标文件全量如下（基于当前 22 行版本）：

```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "model-api-connection"
version = "0.1.0"
description = "Lightweight, config-driven Python connector for multiple LLM providers (OpenAI, Anthropic, Gemini, SiliconFlow, Poe)"
requires-python = ">=3.10"
dependencies = [
    "litellm>=1.40.0",
    "python-dotenv>=0.21.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[project.scripts]
llm-sync-keys = "key_sync:main"

[tool.setuptools]
py-modules = ["model_connector", "video_connector", "gemini_uploader", "paths", "key_sync"]

[tool.setuptools.package-data]
"*" = ["models_config.json"]
```

- [ ] **Step 2: 重新 editable install**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv pip install -e .`

Expected 末尾：`Successfully installed model-api-connection-0.1.0`。

- [ ] **Step 3: 校验命令可执行**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run llm-sync-keys --help`

Expected 开头：
```
usage: llm-sync-keys [-h] [--dry-run] [--provider PROVIDER]
```

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run llm-sync-keys --dry-run`

Expected（顺序可能变化）：
```
- openai       op://llmkeys/OpenAI/credential -> OPENAI_API_KEY
- anthropic    op://llmkeys/Anthropic/credential -> ANTHROPIC_API_KEY
- gemini       op://llmkeys/Gemini/credential -> GEMINI_API_KEY
- siliconflow  op://llmkeys/SiliconFlow/credential -> SILICONFLOW_API_KEY
- poe          op://llmkeys/Poe/credential -> POE_API_KEY
```

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml
git commit -m "$(cat <<'EOF'
feat: register llm-sync-keys console script and expose new py-modules

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 9: `model_connector.py` 改为固定路径 `load_dotenv`

**Files:**
- Modify: `model_connector.py`
- Create: `tests/test_model_connector_loading.py`

- [ ] **Step 1: 写失败测试**

创建 `$WORKSPACE_ROOT/model_api_connection/tests/test_model_connector_loading.py`：

```python
"""Tests for model_connector's auto-loading of ~/.config/llm/keys.env."""

import importlib
import os


def test_import_calls_load_dotenv_with_keys_env_path(tmp_path, monkeypatch):
    fake_keys_env = tmp_path / "keys.env"
    fake_keys_env.write_text("OPENAI_API_KEY=sk-from-cache\n", encoding="utf-8")

    monkeypatch.setattr("paths.KEYS_ENV_PATH", fake_keys_env, raising=True)
    monkeypatch.delenv("OPENAI_API_KEY", raising=False)

    import model_connector

    importlib.reload(model_connector)

    assert os.environ.get("OPENAI_API_KEY") == "sk-from-cache"


def test_import_does_not_override_existing_env_var(tmp_path, monkeypatch):
    fake_keys_env = tmp_path / "keys.env"
    fake_keys_env.write_text("OPENAI_API_KEY=sk-from-cache\n", encoding="utf-8")

    monkeypatch.setattr("paths.KEYS_ENV_PATH", fake_keys_env, raising=True)
    monkeypatch.setenv("OPENAI_API_KEY", "sk-from-shell")

    import model_connector

    importlib.reload(model_connector)

    assert os.environ["OPENAI_API_KEY"] == "sk-from-shell"


def test_import_survives_missing_keys_env(tmp_path, monkeypatch):
    missing = tmp_path / "nope.env"
    monkeypatch.setattr("paths.KEYS_ENV_PATH", missing, raising=True)

    import model_connector

    importlib.reload(model_connector)  # should not raise
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_model_connector_loading.py -v`

Expected: 前两个测试 fail（环境变量为 None / 被 reset）。

- [ ] **Step 3: 修改 `model_connector.py`**

用 Edit 工具把当前 37–54 行：

```python
import json
import os
from pathlib import Path
from typing import Iterator

import litellm

try:
    from dotenv import load_dotenv
    load_dotenv()
except ImportError:
    pass


__all__ = ["LLMConnector", "chat", "get_connector", "strip_think_stream"]

CONFIG_PATH = Path(__file__).parent / "models_config.json"
```

替换为：

```python
import json
import os
from pathlib import Path
from typing import Iterator

import litellm

import paths
from paths import CONFIG_PATH

try:
    from dotenv import load_dotenv
    if paths.KEYS_ENV_PATH.exists():
        load_dotenv(paths.KEYS_ENV_PATH, override=False)
except ImportError:
    pass


__all__ = ["LLMConnector", "chat", "get_connector", "strip_think_stream"]
```

**注意**：import `paths` 而非 `from paths import KEYS_ENV_PATH` 是为了让测试里的 `monkeypatch.setattr("paths.KEYS_ENV_PATH", ...)` 生效（reload 时取的是 module 属性）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_model_connector_loading.py -v`

Expected: 3 passed。

- [ ] **Step 5: 跑回归 —— 老测试依然通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/ -v`

Expected: 所有历史测试 + 新测试全部 pass（不含真实 API 调用的测试）。如果 `tests/` 下有用 `.env` 的集成测试，需确认它们仍能在 `shell export` 或 `keys.env` 情况下跑通。

- [ ] **Step 6: Commit**

```bash
git add model_connector.py tests/test_model_connector_loading.py
git commit -m "$(cat <<'EOF'
refactor: load ~/.config/llm/keys.env at import time (CWD-independent)

Replaces the CWD-based load_dotenv() call with a fixed-path load against
KEYS_ENV_PATH, keeping override=False so shell-exported env still wins.

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 10: 改进"key 找不到"时的错误信息

**Files:**
- Create: `tests/test_missing_key_error.py`
- Modify: `model_connector.py`

- [ ] **Step 1: 写失败测试**

创建 `$WORKSPACE_ROOT/model_api_connection/tests/test_missing_key_error.py`：

```python
"""Error messaging when an API key is not in the environment."""

import pytest

import model_connector


def test_missing_key_error_mentions_llm_sync_keys(monkeypatch):
    monkeypatch.delenv("OPENAI_API_KEY", raising=False)
    llm = model_connector.LLMConnector()

    with pytest.raises(EnvironmentError) as excinfo:
        llm.chat("hi", provider="openai")

    msg = str(excinfo.value)
    assert "OPENAI_API_KEY" in msg
    assert "llm-sync-keys" in msg
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_missing_key_error.py -v`

Expected: fail — 当前错误信息没提 `llm-sync-keys`。

- [ ] **Step 3: 改 `_get_api_key`**

用 Edit 把 `_get_api_key` 里的 `EnvironmentError` block：

旧：
```python
        key = os.environ.get(env_var, "")
        if not key:
            raise EnvironmentError(
                f"API key for '{provider}' not found. "
                f"Set the '{env_var}' environment variable or pass "
                f"api_keys={{'{provider}': '...'}} to LLMConnector()."
            )
        return key
```

新：
```python
        key = os.environ.get(env_var, "")
        if not key:
            raise EnvironmentError(
                f"API key for '{provider}' not found. "
                f"Set the '{env_var}' environment variable, pass "
                f"api_keys={{'{provider}': '...'}} to LLMConnector(), "
                f"or run `llm-sync-keys` to populate "
                f"~/.config/llm/keys.env from 1Password."
            )
        return key
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/test_missing_key_error.py -v`

Expected: 1 passed。

- [ ] **Step 5: Commit**

```bash
git add model_connector.py tests/test_missing_key_error.py
git commit -m "$(cat <<'EOF'
feat: point missing-key errors at llm-sync-keys

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 11: 更新 README、删除旧 `.env.example`

**Files:**
- Modify: `README.md`
- Delete: `.env.example`

- [ ] **Step 1: 用 Edit 把 README.md 的 "配置 API key"、"安装" 相关章节替换**

在 `README.md` 把当前（45–68 行附近）的：

```markdown
## 安装

**1. 安装依赖**

​```bash
pip install -r requirements.txt
# 或安装为包：
pip install -e .
​```

**2. 配置 API key**

​```bash
cp .env.example .env
# 编辑 .env 填入你的 key
​```

或直接导出环境变量：

​```bash
export OPENAI_API_KEY=sk-...
export ANTHROPIC_API_KEY=sk-ant-...
export GEMINI_API_KEY=AIza...
export SILICONFLOW_API_KEY=sf-...
export POE_API_KEY=...
​```
```

替换为：

```markdown
## 安装

**1. 安装包（推荐 uv）**

​```bash
# 在任意消费方项目的 repo 根目录执行
uv add --editable ~/workspace/model_api_connection
# 或老式做法
pip install -e ~/workspace/model_api_connection
​```

**2. 配置 API key（1Password + 本地缓存）**

密钥真相源是 1Password vault `llmkeys`（每个 provider 一条，字段名 `credential`）。程序通过本地缓存 `~/.config/llm/keys.env` 读取，不直接访问 1P。

首次在每台 Mac 上跑一次：

​```bash
# 前置
brew install 1password 1password-cli uv

# 启用 1P App -> Settings -> Developer -> Integrate with 1Password CLI

# 拉取所有 key 到本地缓存
llm-sync-keys
​```

`llm-sync-keys` 会把 5 个 provider 的 key 写入 `~/.config/llm/keys.env`（自动 `chmod 600`）。connector 在 import 时自动读这份文件，无视 CWD。

**轮换单个 key**：在 1P UI 改条目，然后 `llm-sync-keys --provider openai`。

**看不到实际拉了啥**：`llm-sync-keys --dry-run`。
```

把文件末尾的"给 AI Agent 的调用指南"一段里的安装示例 `pip install -e` 也同步改成 `uv add --editable`（保留 pip 注释做兜底）。

- [ ] **Step 2: 删除 `.env.example`**

```bash
cd $WORKSPACE_ROOT/model_api_connection
git rm .env.example
```

- [ ] **Step 3: 校验 README 渲染正常**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run python -c "import pathlib, re; r = pathlib.Path('README.md').read_text(); assert 'llm-sync-keys' in r and 'llmkeys' in r and 'uv add --editable' in r; print('README ok')"`

Expected: `README ok`。

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: rewrite setup section for 1Password + llm-sync-keys flow

Drops .env.example (replaced by the 1P llmkeys vault + sync command).

Generated with [Claude Code](https://claude.ai/code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
EOF
)"
```

---

## Task 12: 本机首次 `llm-sync-keys` + 清理旧 `.env`

**Note**：这是"人工验收"节点。用户必须在本机上交互式执行一次。

**Files:**
- Delete: `.env`（仓库根的明文密钥文件）

- [ ] **Step 1: 前提条件检查**

用户手动确认：
1. 1P App 装好、登录了本人账户
2. 1P App Settings → Developer → "Integrate with 1Password CLI" 已勾选
3. 1P vault `llmkeys` 存在，已有 5 条条目：`OpenAI`、`Anthropic`、`Gemini`、`SiliconFlow`、`Poe`，每条 `credential` 字段填了真实 key
4. 装了 `brew install 1password-cli`（命令 `op --version` 能跑通）

- [ ] **Step 2: 跑 `llm-sync-keys`**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run llm-sync-keys`

Expected：会弹 Touch ID 指纹框，然后输出：
```
  OK  openai       -> OPENAI_API_KEY
  OK  anthropic    -> ANTHROPIC_API_KEY
  OK  gemini       -> GEMINI_API_KEY
  OK  siliconflow  -> SILICONFLOW_API_KEY
  OK  poe          -> POE_API_KEY

Wrote 5 keys to $HOME/.config/llm/keys.env (chmod 600).
```

- [ ] **Step 3: 校验缓存文件**

Run: `ls -la ~/.config/llm/keys.env && stat -f "%Sp %N" ~/.config/llm/keys.env`

Expected：文件存在，权限显示 `-rw-------`。

- [ ] **Step 4: 在一个无关目录下 smoke test connector**

Run:
```bash
cd /tmp && uv --project $WORKSPACE_ROOT/model_api_connection run python -c "
from model_connector import chat
print(chat('Return the single word PING.', provider='openai', model='gpt-4o-mini'))
"
```

Expected：返回一行包含 `PING` 的回复（或极简近似）。如果报 API error，先看 `~/.config/llm/keys.env` 内容是否真的含 `OPENAI_API_KEY`。

- [ ] **Step 5: 删掉仓库里旧的 `.env`**

```bash
cd $WORKSPACE_ROOT/model_api_connection
ls -la .env 2>/dev/null && rm .env || echo "(no .env present)"
```

确认 `git status` 没有要提交的变更（`.env` 本来就在 `.gitignore` 里，所以不会出现在 git tree）。

- [ ] **Step 6: 验证 `launchd` 场景（可选、推荐）**

写一个临时 shell 脚本 `/tmp/llm-cron-smoke.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
cd $WORKSPACE_ROOT/model_api_connection
uv run python -c "from model_connector import chat; print(chat('say OK', provider='openai', model='gpt-4o-mini'))"
```

`chmod +x /tmp/llm-cron-smoke.sh`，用 `launchctl` 提交一个一次性任务或手动 `env -i /tmp/llm-cron-smoke.sh`（`env -i` 清空环境变量，模拟 cron）。

Expected：脚本在无 shell 环境变量情况下仍然能跑通（因为 connector 从 `~/.config/llm/keys.env` 加载）。

- [ ] **Step 7: No commit**（这一步主要是人工验收；如果要记录验收结果，可以开一个 issue/note，不进 git 历史）

---

## Task 13: 整体冒烟 + 推分支

**Files:** 无

- [ ] **Step 1: 跑完整测试**

Run: `cd $WORKSPACE_ROOT/model_api_connection && uv run pytest tests/ -v`

Expected：全部 pass。如果有之前（非本次引入）的 flaky/integration 测试失败，单独记录，不阻塞。

- [ ] **Step 2: lint / pre-commit 再跑一次**

Run: `cd $WORKSPACE_ROOT/model_api_connection && git status && git log --oneline refactor/1p-key-sync ^main`

Expected：工作树 clean；分支 commit 历史按本 plan 的顺序排布，每条都带 co-author。

- [ ] **Step 3: 推分支（等用户确认后）**

询问用户是否推分支 / 开 PR。推荐：

```bash
git push -u origin refactor/1p-key-sync
```

用户确认后再跑。

- [ ] **Step 4: 更新 `MEMORY.md`（可选）**

如果这次 redesign 产生了需要长期记忆的用户偏好（比如"用户的 LLM 密钥一律放 1P vault llmkeys"），在 auto-memory 里加条 reference memory。
